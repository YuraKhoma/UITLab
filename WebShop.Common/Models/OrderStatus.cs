namespace WebShop.Common.Models
{
    /// <summary>
    /// Enum of orer status
    /// </summary>
    public enum OrderStatus
    {
        New,
        Canceled,
        Payed,
        Sent,
        Received,
        Completed
    }
}
