namespace WebShop.Common.Models
{
    /// <summary>
    /// Enum of status of items
    /// </summary>
    public enum ItemStatus
    {
        New,
        Canceled,
        Payed,
        Sent,
        Received,
        Completed
    }
}
